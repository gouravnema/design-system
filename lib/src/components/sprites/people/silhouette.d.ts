export interface PeopleProps {
    rounded?: boolean;
    size?: number;
    code: string;
}
export declare const Silhouette: ({ code, rounded, size }: PeopleProps) => import("react/jsx-runtime").JSX.Element | null;
